coverage:
  precision: 2
  round: down
  range: 80...100
  status:
    project:
      default:
        flags:
          - unit
          - end_to_end
      unit:
        flags:
          - unit
      # integration:
      #   flags:
      #     - integration
      end_to_end:
        flags:
          - end_to_end

ignore:
  - ".tox/**/*"
  - "setup.py"
